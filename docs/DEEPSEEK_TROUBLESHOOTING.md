# 🔧 Устранение проблем DeepSeek

## Быстрая диагностика

### 1. Проверьте конфигурацию

Откройте в браузере:
```
http://localhost:3000/api/test-deepseek
```

Этот endpoint покажет:
- ✅ Установлен ли `USE_DEEPSEEK=true`
- ✅ Есть ли `DEEPSEEK_API_KEY`
- ✅ Работает ли DeepSeek API
- ❌ Подробности ошибки (если есть)

---

## Частые проблемы и решения

### ❌ Проблема 1: "DEEPSEEK_API_KEY не установлен"

**Причина:** Переменная окружения отсутствует или сервер не перезапущен после её добавления.

**Решение:**

1. Откройте `.env.local` и проверьте:
   ```bash
   USE_DEEPSEEK=true
   DEEPSEEK_API_KEY=sk-ваш-ключ-здесь
   DEEPSEEK_MODEL=deepseek-chat
   ```

2. **ВАЖНО!** Перезапустите dev сервер:
   ```bash
   # Остановите (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

3. Проверьте снова: http://localhost:3000/api/test-deepseek

---

### ❌ Проблема 2: "DeepSeek API error: 401 Unauthorized"

**Причина:** Неверный API ключ.

**Решение:**

1. Проверьте ключ на https://platform.deepseek.com/api_keys
2. Убедитесь, что ключ **активен** и **не истёк**
3. Скопируйте ключ заново в `.env.local`
4. Перезапустите сервер

---

### ❌ Проблема 3: "DeepSeek API error: 429 Too Many Requests"

**Причина:** Превышен rate limit (лимит запросов).

**Решение:**

**Опция A: Подождать**
- Дождитесь сброса лимита (обычно через минуту)

**Опция Б: Временно использовать mock**
- В `.env.local` установите:
  ```bash
  USE_DEEPSEEK=false
  ```
- Перезапустите сервер

---

### ❌ Проблема 4: "Неожиданный формат ответа от DeepSeek API"

**Причина:** DeepSeek изменил формат API или модель недоступна.

**Решение:**

1. Проверьте, что модель существует:
   ```bash
   DEEPSEEK_MODEL=deepseek-chat  # рекомендуемая
   ```

2. Посмотрите актуальный список моделей: https://api-docs.deepseek.com/

3. Проверьте диагностику: http://localhost:3000/api/test-deepseek

---

### ❌ Проблема 5: Автоматический fallback на mock

**Симптомы:** 
- В консоли сервера видно: "DeepSeek failed, falling back to mock"
- Всегда возвращаются одинаковые темы из mock

**Причина:** DeepSeek API недоступен, система переключилась на mock.

**Решение:**

1. Посмотрите полный текст ошибки в консоли dev сервера
2. Откройте диагностику: http://localhost:3000/api/test-deepseek
3. Проверьте интернет соединение
4. Проверьте, что https://api.deepseek.com доступен

---

### ❌ Проблема 6: Сервер не видит изменения .env.local

**Причина:** Next.js кэширует переменные окружения при старте.

**Решение:**

1. **Полная очистка кэша:**
   ```bash
   # Остановите сервер (Ctrl+C)
   
   # Удалите .next (кэш Next.js)
   rm -rf .next
   # или в Windows PowerShell:
   Remove-Item -Recurse -Force .next
   
   # Запустите снова
   npm run dev
   ```

2. Проверьте, что изменения применились:
   ```
   http://localhost:3000/api/test-deepseek
   ```

---

## Проверка логов

### В консоли браузера (F12)

1. Откройте DevTools (F12)
2. Вкладка **Network**
3. Отправьте запрос на генерацию темы
4. Найдите запрос `/api/spin`
5. Посмотрите **Response** (что вернул сервер)

### В консоли dev сервера

Смотрите терминал, где запущен `npm run dev`. Ошибки будут выглядеть так:

```
Error in generateRandomTopic: DeepSeek API error: 401 Unauthorized
DeepSeek failed, falling back to mock
```

---

## Тестирование вручную

### Через curl (если доступен)

```bash
# Тест DeepSeek API напрямую
curl -X POST https://api.deepseek.com/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-ваш-ключ" \
  -d '{
    "model": "deepseek-chat",
    "messages": [
      {"role": "system", "content": "Ты помощник."},
      {"role": "user", "content": "Скажи привет"}
    ]
  }'
```

### Через браузер

1. Откройте http://localhost:3000/api/test-deepseek
2. Должен вернуться JSON с диагностикой

---

## Пошаговая проверка

1. ☐ **Конфигурация**
   - Откройте `.env.local`
   - Проверьте `USE_DEEPSEEK=true`
   - Проверьте наличие `DEEPSEEK_API_KEY`

2. ☐ **Перезапуск**
   - Остановите сервер (Ctrl+C)
   - Запустите `npm run dev`

3. ☐ **Диагностика**
   - Откройте http://localhost:3000/api/test-deepseek
   - Смотрите результат теста

4. ☐ **Тест генерации**
   - Откройте приложение
   - Войдите с любым ФИО
   - Нажмите "Выбрать тему"
   - Смотрите консоль браузера (F12) и сервера

5. ☐ **Логи**
   - Если ошибка - скопируйте полный текст
   - Проверьте консоль сервера на предупреждения

---

## Контакты для помощи

Если проблема не решается:

1. Скопируйте вывод `/api/test-deepseek`
2. Скопируйте логи из консоли dev сервера
3. Скопируйте ошибки из консоли браузера (F12 → Console)
4. Проверьте документацию DeepSeek: https://api-docs.deepseek.com

---

## Полезные ссылки

- 🔗 [DeepSeek API Keys](https://platform.deepseek.com/api_keys)
- 🔗 [DeepSeek API Docs](https://api-docs.deepseek.com)
- 🔗 [DeepSeek Usage](https://platform.deepseek.com/usage)
- 🔗 [DeepSeek Pricing](https://platform.deepseek.com/pricing)
