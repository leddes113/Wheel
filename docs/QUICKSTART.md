# Быстрый старт: DeepSeek интеграция

## ✅ Созданные файлы

### 1. **lib/llm/deepseek.ts** (новый)
- Интеграция с DeepSeek API
- Вызов `POST https://api.deepseek.com/chat/completions`
- Системные промпты для генерации и проверки тем
- Парсинг ответов в структурированный формат

### 2. **lib/llm/index.ts** (новый)
- Unified интерфейс LLM
- Переключение между mock и DeepSeek через `USE_DEEPSEEK`
- Экспорт: `generateRandomTopic()`, `validateOwnIdea()`

### 3. **lib/gigachat-mock.ts** (обновлен)
- Добавлена логика для абстрактных идей
- Словарь абстрактных слов: "собака", "кот", "машина" и др.
- Возвращает 3-5 конкретных вариантов с полной структурой

### 4. **app/api/spin/route.ts** (обновлен)
- Импорт изменен с `@/lib/gigachat-mock` на `@/lib/llm`

### 5. **app/api/idea/route.ts** (обновлен)
- Импорт изменен с `@/lib/gigachat-mock` на `@/lib/llm`

### 6. **.env.example** (новый)
- Примеры переменных окружения
- `USE_DEEPSEEK` - флаг переключения
- `DEEPSEEK_API_KEY` - API ключ
- `DEEPSEEK_MODEL` - модель (default: deepseek-chat)

### 7. **docs/DEEPSEEK_SETUP.md** (новый)
- Подробная документация
- Примеры запросов и ответов
- Troubleshooting

---

## 🚀 Как проверить локально

### Вариант 1: Mock режим (без API ключа)

```bash
# 1. Создайте .env файл
cp .env.example .env

# По умолчанию USE_DEEPSEEK=false (mock режим)

# 2. Запустите сервер
npm install
npm run dev

# 3. Тестовый запрос
curl -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d '{"fio": "Тест", "level": "beginner"}'

curl -X POST http://localhost:3000/api/idea \
  -H "Content-Type: application/json" \
  -d '{"fio": "Тест", "idea": "собака"}'
```

**Ожидаемый результат:** 5 конкретных вариантов про собак (приложение для выгула, база пород и т.д.)

---

### Вариант 2: DeepSeek режим (с API ключом)

```bash
# 1. Получите API ключ
# Зарегистрируйтесь на https://platform.deepseek.com
# Создайте API ключ

# 2. Создайте .env файл
cp .env.example .env

# 3. Отредактируйте .env
USE_DEEPSEEK=true
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_MODEL=deepseek-chat

# 4. Запустите сервер
npm run dev

# 5. Тестовый запрос
curl -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d '{"fio": "Тест", "level": "beginner"}'

curl -X POST http://localhost:3000/api/spin \
  -H "Content-Type: application/json" \
  -d '{"fio": "Тест"}'
```

**Ожидаемый результат:** Уникальная тема, сгенерированная DeepSeek

---

## 🧪 Тестовые сценарии

### Тест 1: Абстрактная идея отклоняется

```bash
# Отправить идею "собака"
curl -X POST http://localhost:3000/api/idea \
  -H "Content-Type: application/json" \
  -d '{"fio": "Пользователь1", "idea": "собака"}'

# Ожидаемо:
# - suitable: false
# - suggestions: 5 конкретных вариантов
```

### Тест 2: Конкретная идея принимается

```bash
# Отправить конкретную идею
curl -X POST http://localhost:3000/api/idea \
  -H "Content-Type: application/json" \
  -d '{"fio": "Пользователь2", "idea": "Приложение для учёта личных финансов с категориями расходов"}'

# Ожидаемо:
# - suitable: true
# - topic: "Приложение для учёта..."
# - advice: 3 совета
```

### Тест 3: Случайная тема генерируется

```bash
# Получить случайную тему
curl -X POST http://localhost:3000/api/spin \
  -H "Content-Type: application/json" \
  -d '{"fio": "Пользователь3"}'

# Ожидаемо:
# - success: true
# - topic: сгенерированная тема
# - deadline: через 14 дней
```

---

## 📋 Структура файлов

```
vibe-wheel/
├── lib/
│   ├── llm/
│   │   ├── deepseek.ts      ← DeepSeek API
│   │   └── index.ts          ← Unified интерфейс
│   ├── gigachat-mock.ts      ← Mock LLM (обновлен)
│   ├── storage.ts
│   └── types.ts
├── app/
│   └── api/
│       ├── spin/route.ts     ← Обновлен
│       └── idea/route.ts     ← Обновлен
├── docs/
│   ├── context.md
│   ├── DEEPSEEK_SETUP.md     ← Подробная документация
│   └── QUICKSTART.md         ← Этот файл
├── .env.example              ← Шаблон переменных окружения
└── .env                      ← Ваша конфигурация (создайте)
```

---

## 🔧 Переменные окружения

| Переменная | Обязательна | Значение по умолчанию | Описание |
|------------|-------------|----------------------|----------|
| `USE_DEEPSEEK` | Нет | `false` | Использовать DeepSeek (`true`) или mock (`false`) |
| `DEEPSEEK_API_KEY` | Да (если `USE_DEEPSEEK=true`) | - | API ключ с platform.deepseek.com |
| `DEEPSEEK_MODEL` | Нет | `deepseek-chat` | Модель: `deepseek-chat` или `deepseek-coder` |

---

## ⚠️ Важные моменты

1. **Mock режим по умолчанию**  
   Если `.env` не создан или `USE_DEEPSEEK` не установлен → используется mock LLM

2. **Детерминированное поведение в mock**  
   Mock режим всегда возвращает одинаковые результаты для одинаковых входов (для тестов)

3. **Абстрактные идеи**  
   Идеи типа "собака", "машина" отклоняются с конкретными вариантами в обоих режимах

4. **Формат вариантов**  
   Каждый вариант содержит:
   - Название
   - Описание (20-40 слов)
   - Ключевые фичи
   - Какие данные хранить
   - Критерий готовности проекта

5. **Fallback на mock**  
   Если DeepSeek недоступен и `USE_DEEPSEEK=false` → автоматический переход на mock

---

## 📚 Дополнительная документация

- **Полная инструкция:** `docs/DEEPSEEK_SETUP.md`
- **Контекст проекта:** `docs/context.md`
- **DeepSeek API:** https://api-docs.deepseek.com

---

## ✨ Готово!

Интеграция DeepSeek завершена. Система поддерживает:
- ✅ Реальный LLM (DeepSeek)
- ✅ Mock LLM (для тестов)
- ✅ Переключение через флаг
- ✅ Отклонение абстрактных идей
- ✅ Конкретные варианты в структурированном формате
